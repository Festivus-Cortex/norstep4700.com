/**
 * Effect Configuration System
 *
 * Centralized configuration for the effect system with support for:
 * - Audio analysis settings (FFT, frequency bands, normalization)
 * - Effect-specific defaults and overrides
 * - Intensity-based parameter variations
 * - Named variants for common configurations
 */

export * from "./types";
export * from "./loader";
export * from "./resolver";
